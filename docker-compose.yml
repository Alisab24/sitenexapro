version: '3'
services:
  docuseal:
    image: docuseal/docuseal:latest
    ports:
      - "3000:3000"
    volumes:
      - ./data:/data
    environment:
      - SECRET_KEY_BASE=nexapro2025signatureelectronique64chars!!
    restart: unless-stopped
